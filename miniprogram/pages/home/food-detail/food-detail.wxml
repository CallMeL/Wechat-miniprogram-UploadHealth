<view>
	<button bindtap="addFoodRecord">添加食物到我的</button>
	<text>食物名称：{{foodName}}</text>
	<view class = "Detail-Container" wx:for="{{foodDetail}}"> {{item.name}}:{{item.value}}{{item.unit}}</view>
	<button bindtap="showPicker_03">选择添加份量</button>
<text>份量：{{howmany}}</text>
</view>

<view>选择数据：{{picker_03_data}}</view>
<view>选择索引：{{picker_03_index}}</view>
<picker
	isShowPicker="{{isShow_03}}"
	bindsure="sureCallBack_03"
	bindcancle="cancleCallBack_03"
	scrollType="normal"
  indicatorStyle="height:40%"
  titleText="选择份量"
	listData="{{listData}}"
></picker>

